# URL pública de API Gateway (output ApiUrl de CloudFormation), SIN "/" al final.
# En Amplify se define como variable de entorno del build; en local, copia este
# archivo como .env.local.
VITE_API_BASE_URL=https://XXXXXXXX.execute-api.us-east-1.amazonaws.com
